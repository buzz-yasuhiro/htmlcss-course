title: HTML＆CSS言語を学ぶ旅
description: HTML　CSSを学ぶ
template:
  repo: htmlcss-template
  name: htmlcss
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
